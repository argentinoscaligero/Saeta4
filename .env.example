# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=saeta
DB_PASSWORD=your_secure_password_here
DB_NAME=saeta4

# JWT Configuration
# Generate secure key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-me-in-production-use-command-above-to-generate
JWT_EXPIRATION=1d

# Application Configuration
PORT=3000

# Production Server (saeta.penaltycorner.com.ar)
# - OS: Oracle Linux ARM64
# - Database User: saeta
# - Database Name: saeta4
# - App runs on port 3000 (Nginx proxies 443 → 3000)
